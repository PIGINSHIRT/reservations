<view class="page">
  <view class="header">
    <view class="user-info">
      <view class="avatar" style="background-color: #fff; display: flex; align-items: center; justify-content: center; font-size: 50rpx;">
        🐻
      </view>
      
      <view style="display: flex; flex-direction: column;">
        <block wx:if="{{hasUserInfo && userInfo.phone}}">
          <text class="phone-number">{{userInfo.phone}}</text>
        </block>
        <block wx:elif="{{hasUserInfo}}">
          <text class="phone-number" style="font-size: 32rpx; opacity: 0.9;">新同学 (未绑定手机)</text>
        </block>
        <block wx:else>
          <text class="phone-number">登录中...</text>
        </block>
      </view>
    </view>
    <text class="welcome-msg">欢迎来「Go!Study!自习室」学习！</text>
  </view>

  <view class="logo-section">
    <text class="logo-mascot">🐻</text>
    <text class="logo-text">GO!Study!</text>
    <text class="logo-subtitle">沉浸式自习室</text>
  </view>

  <view class="action-buttons">
    <view class="action-btn">
      <text class="action-icon">👤</text>
      <text class="action-text">个人信息</text>
    </view>
    <view class="action-btn" wx:if="{{userInfo.isAdmin}}">
      <text class="action-icon">🔧</text>
      <text class="action-text">管理员后台</text>
    </view>
    <view class="action-btn" wx:else>
      <text class="action-icon">🚪</text>
      <text class="action-text">退出登录</text>
    </view>
  </view>

  <view class="orders-card">
    <view class="orders-header">
      <text class="orders-icon">🎫</text>
      <text class="orders-title">我的卡券订单</text>
    </view>
    <view style="padding: 20rpx 0; color: #999; font-size: 28rpx;">
      暂无有效卡券，快去首页购买吧~
    </view>
  </view>
</view>